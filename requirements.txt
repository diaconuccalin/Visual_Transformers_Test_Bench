# Core dependencies
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
pillow>=9.0.0
tqdm>=4.65.0

# For original Visual Wake Words dataset (MLPerf compatible)
pyvww>=0.1.0

# Optional: For Wake Vision dataset (alternative, 100x larger)
# Uncomment if you want to use Wake Vision instead of original VWW
# tensorflow-datasets>=4.9.0
# tensorflow>=2.12.0
